-# Chapt 11.3.2 REFACTOR - Added micropost partial if user is signed in
- if signed_in?
  %table.front{ :summary => "For signed-in users" }
    %tr
      %td.main
        %h1.micropost What's up?
        = render 'shared/micropost_form'
        = render 'shared/feed'
      %td.sidebar
        = render 'shared/user_info'
        = render 'shared/stats'
- else
  %h1 Sample App
  %p
    This is the home page for the
    %a{:href => "http://railstutorial.org/", :target => "_blank"} Ruby on rails Tutorial sample application.
  - if !signed_in?
    =link_to "Sign up Now!", signup_path, :class => "signup_button round"